# Supabase Configuration (Required for /schedule page)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Email Configuration (Required for contact and schedule forms)
EMAIL_AUTH_USER=your_email@example.com
EMAIL_AUTH_PASS=your_email_app_password
ADMIN_EMAIL=admin@example.com

# Instructions:
# 1. Copy this file to .env.local for local development
# 2. Fill in your actual values
# 3. Add these environment variables to your Cloudflare Pages deployment settings
# 4. Never commit .env.local or .env files with real values to git